
    <form method="get" action="/admin/cuestionarios/<%= cuestionario.id %>/preguntas/<%= pregunta.id %>/answer">
      <p>Pregunta: <%= pregunta.enunciado %> </p>
      <input type="text" name="respuesta" value="Responda aquí"/>
      <input type="submit" value="Enviar">
    </form>

<p><a href="/admin/preguntas/<%= pregunta.id %>/comments/new"><button>Nuevo comentario</button></a></p>

<p><strong>Comentarios:</strong></p>

<%for(index in pregunta.Comments){%>
	<%if(pregunta.Comments[index].publicado || session.user) {%>
		<p>- <%=pregunta.Comments[index].texto%></p>
		<%if(!pregunta.Comments[index].publicado && session.user) {%>
			<a href="/admin/preguntas/<%= pregunta.id %>/comments/<%= pregunta.Comments[index].id%>/publish"><button>Publicar</button></a>
		<%}%>
		</p>
	<%}%>
<%}%>

